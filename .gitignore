# 隔离层副本（单一源：shared/dbBase.js / userBase.js）
# 由 scripts/bundle-db-base.js 生成；初期开发阶段入库，确保微信开发者工具直传云函数时
# helpers/ 下含 dbBase.js / userBase.js，云端运行时可正常 require('./userBase')。
# 后续若改为 CI 流水线部署，可恢复忽略（改为部署前自动 bundle）。
# cloudfunctions/*/helpers/dbBase.js
# cloudfunctions/*/helpers/userBase.js

# 依赖与构建产物
node_modules/
*.log
.DS_Store

# 本地开发环境（不入库）
.claude/
wechatide_files/
工器具项目书.docx

# 交付产物快照（勿提交进仓库）
AISND-src-*.zip
AISND-full.bundle
