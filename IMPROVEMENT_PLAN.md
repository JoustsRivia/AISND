# AISND 小程序 · 全面改进建议报告

> 基于源代码审查（149 文件 / 18 云函数 / 14 分包）与 AGENTS.md 四项准则对齐分析

---

## 一、代码优化（7 项）

### OPT-01 | 消除跨云函数重复代码
| 状态 | **✅ 已完成（shared/employeeId+crypto+roles 去重）** |

| 项 | 内容 |
| --- | --- |
| 问题 | `generateEmployeeId` 在 `auth/index.js` L57–93 和 `system/index.js` L62–108 各独立实现一份（算法完全相同），`hashPwd` 同理（`auth/index.js` L53, `system/index.js` L56）。`CAT_NAME` / `CAT_ABBR` 在 `tool/index.js` 与 `utils/constants.js` `TOOL_CATEGORIES` 存在语义重叠但未统一。`ROLE_TEXT` 在 `pages/index/index.js` L8–10, `pages/scan/scan.js` L6–9 两处重复定义。违反 AGENTS.md §1②「为下一个需求设计」。 |
| 方案 | ① 提取 `shared/employeeId.js`（纯函数），由 `bundle-db-base.js` 同步到 18 云函数；② `hashPwd` 升入 `shared/crypto.js` 并在升级 PBKDF2 时一并替换（关联 SEC-01）；③ `CAT_NAME`/`CAT_ABBR` 移至 `utils/constants.js`，`tool/index.js` 改为 `require` 引用；④ `ROLE_TEXT` 提取为 `utils/constants.js` 的 `ROLE_LABELS`，全仓统一引用。 |
| 效果 | 未来修改工号规则/密码算法/类别映射只需改一处，杜绝双端漂移；代码行减少约 80 行。 |
| 难度 | **低** — 纯函数提取，Agent 可直接完成 |

### OPT-02 | 限流中间件全量接入（17 个云函数补装）
| 状态 | **✅ 已完成（18 云函数全量接入限流）** |

| 项 | 内容 |
| --- | --- |
| 问题 | `shared/rateLimiter.js` 已实现并同步到所有云函数 helpers/，但仅在 `system/index.js` 实际使用 `wrap()`。其余 17 个云函数的入口未包装限流——auth 登录可被爆破，tool 列表可被刷量，borrow 写操作可被滥用。违反 AGENTS.md §1④「直取核心」——工具已有但没装到位。 |
| 方案 | 为每个云函数 `exports.main` 的 handler 包装 `rateLimiter.createRateLimiter().wrap(handler, fnName)`：auth → login/signin/register 窗口 60s≤5；borrow/scrap/purchase → 窗口 60s≤30；查询类 → 窗口 60s≤60。 |
| 效果 | 全端点频率保护，消除暴力破解与 API 滥用面；可与后台限流看板联动监控。 |
| 难度 | **低** — 模板化改动，每个云函数 3 行代码 |

### OPT-03 | 内存缓存主动 TTL 轮换
| 状态 | **✅ 已完成（system 缓存主动 TTL 轮换）** |

| 项 | 内容 |
| --- | --- |
| 问题 | `system/index.js` L14–23 的 `__retentionCache` 和 L36–45 的 `__rateCache` 仅在读取时被动校验 60s 窗口，云函数实例冻结后恢复时可能持有过期缓存超出预期时间。 |
| 方案 | 初始化时用 `setInterval` 每 60s 主动 `clearRetentionCache()` / `clearRateCache()`，并在云函数入口结束时 `clearInterval`。或在 `config.json` 加定时触发器定期自刷新。 |
| 效果 | 策略变更后最多 60s 内在所有实例生效，避免实例冻结导致的配置延迟。 |
| 难度 | **低** — 4 行代码 |

### OPT-04 | Scaffold 模板对齐正式函数模式
| 状态 | **✅ 已完成（scaffold 模板对齐 dbBase+rateLimiter）** |

| 项 | 内容 |
| --- | --- |
| 问题 | `scaffolds/tpl/helpers/db.js` L4–7 直接 `require('wx-server-sdk')`，而未通过 `dbBase` 隔离层引用。从模板创建的新云函数不享受迁移保护。且 `scaffolds/tpl/helpers/` 缺少 `rateLimiter.js`。 |
| 方案 | ① `scaffolds/tpl/helpers/db.js` 改为 `const base = require('./dbBase');` 模式；② `shared/rateLimiter.js` 加入模板并纳入 `bundle-db-base.js` 同步清单；③ `README.md` 补充新云函数接入限流的创建规范。 |
| 效果 | 新云函数从模板创建即具备隔离层迁移能力 + 限流能力，消除新人操作风险。 |
| 难度 | **低** — 3 文件修改 |

### OPT-05 | 前端子树推导函数去重
| 状态 | **✅ 已完成（utils/org-utils subtreeIds 去重）** |

| 项 | 内容 |
| --- | --- |
| 问题 | `subtreeIds` 在 `pages/ledger/ledger.js` L19–29 有客户端版本 `clientSubtree`，与 `shared/dbBase.js` L42–53 的服务端版算法完全相同。暴露了客户端也需要子树推导的重复实现。 |
| 方案 | 在 `utils/` 下新增 `org-utils.js`，导出客户端版 `subtreeIds` 和 `buildOrgPicker`，ledger/profile 等页面统一引用。 |
| 效果 | 子树推导逻辑统一，减少 10 行重复代码。 |
| 难度 | **低** |

### OPT-06 | 全局常量收敛
| 状态 | **✅ 已完成（shared/roles.js 统一角色白名单）** |

| 项 | 内容 |
| --- | --- |
| 问题 | `ROLE_WHITE` 在 `system/index.js` L155 硬编码，`SELF_BINDABLE_ROLES` 在 `auth/index.js` L11 硬编码——两处定义的含义相近（"可自助绑定的角色白名单"）但因上下文不同变成了两套独立维护的常量。 |
| 方案 | 统一收敛到 `utils/constants.js`：① `SYSTEM_ROLES`（系统全部角色）→ 已有 `ROLES`；② `SELF_BINDABLE_ROLES`（可自助绑定的角色）→ 新增；③ `ROLE_WHITE`（系统管理可用角色）→ 与 `SELF_BINDABLE_ROLES` 合并，统一维护。`system/index.js` 和 `auth/index.js` 改为 `require` 引用。 |
| 效果 | 新增角色时只需改一处（constants.js），编译期即可发现引用位点，避免运行时才发现权限遗漏。 |
| 难度 | **低** — 3 文件修改 |

### OPT-07 | 云函数查询硬编码上限改造为真分页
| 状态 | **✅ 已完成（shared/dbBase.listAll 真分页）** |

| 项 | 内容 |
| --- | --- |
| 问题 | 多处 `listBy('orgs', {}, 500)` 和 `listBy('users', {}, 500)` 硬编码上限。微信云数据库实际单次 limit 默认 100，超过需循环拉取。真实用户 >500 时 `generateEmployeeId` 可能遗漏工号导致编号碰撞，`orgManage` 在组织 >500 时层级计算不准。 |
| 方案 | 在 `shared/dbBase.js` 新增 `listAll(collectionName, filter, opts)` → 内部循环 `.skip().limit(100)` 直到无可获取数据，或改用云数据库聚合搜索 `aggregate`。此 PR 可先改造 `orgTree` / `generateEmployeeId` / `userManage list` 三处高风险点。 |
| 效果 | 支撑组织 / 用户规模 >500 的场景，消除数据不完整导致的功能异常。 |
| 难度 | **中** — 需确认云数据库 API 的分页行为与性能边界 |

---

## 二、现有功能改进（8 项）

### FEAT-01 | 登录密码复杂度校验缺失
| 状态 | **✅ 已完成（密码强度校验 服务端+客户端）** |

| 项 | 内容 |
| --- | --- |
| 问题 | `auth/index.js` register 和 `system/index.js` userManage 对密码无任何复杂度要求——用户可以设 `123` 为密码。客户端 `login.js` 也无输入校验提示。这是一个安全隐患，更是一个用户体验问题：用户在弱密码下被攻击后，审计日志将无明确阻断记录。 |
| 方案 | ① 服务端 register/update 增加密码强度校验：最小 6 位 + 至少含字母/数字两种类型；② 客户端 `login.js` 增加即时校验提示（密码框下方红色文字）；③ 登录页增加「锁定策略」：连续 5 次失败锁定 15 分钟。 |
| 效果 | 显著降低弱密码攻击面，满足等保三级密码策略要求。 |
| 难度 | **低** |
| 优先级 | **P0（安全基线）** |

### FEAT-02 | 离线场景无本地缓存
| 状态 | **✅ 已完成（cacheThenNetwork 离线缓存）** |

| 项 | 内容 |
| --- | --- |
| 问题 | `pages/index/index.js` 在 `onLoad` 全量拉取 `getHomeStatus()` + `getDashboard()`，网络中断时用户看到的是空白页 + 加载动画。施工现场弱网/无网是常态（地下室、隧道、偏远工地），当前仅 `network.requireOnline()` 做简单拦截，无降级方案。 |
| 方案 | ① `utils/network.js` 增加 `cacheThenNetwork` 策略：先从 `wx.getStorageSync` 读上次缓存展示，后台静默拉取最新数据覆盖；② 首页 dashboard + 台账统计卡 + 消息列表均接入此策略；③ 缓存附带时间戳，超过 24h 显示「数据可能不是最新」提示。 |
| 效果 | 弱网下用户仍能看到上次操作后的数据，不会因网络抖动完全白屏。 |
| 难度 | **中** — 涉及 3–4 个页面的数据层改造 |
| 优先级 | **P1（现场刚需）** |

### FEAT-03 | 搜索功能仅匹配名称
| 状态 | **✅ 已完成（多字段 OR 搜索）** |

| 项 | 内容 |
| --- | --- |
| 问题 | `tool/index.js` list 关键词搜索仅匹配 `name` 字段（L107）。现场常见的搜索需求是"输入编号查器具"（`code`），目前不支持。搜索结果 0 条时用户会误以为器具不存在。 |
| 方案 | 关键词搜索改为多字段 OR 匹配：`name` + `code` + `store`（库房名） + `keeper`（保管人） + `factoryNo`（出厂编号）。使用云数据库 `_.or` 组合条件，保持大小写不敏感。 |
| 效果 | 现场人员按任一已知信息（编号/保管人/库房）均可检索到器具，降低"找不到"的求助频次。 |
| 难度 | **低** — 单函数 1 行改动 |
| 优先级 | **P0** |

### FEAT-04 | 扫码历史仅存内存，应用被杀即丢失
| 状态 | **✅ 已完成（扫码历史持久化）** |

| 项 | 内容 |
| --- | --- |
| 问题 | `pages/scan/scan.js` L53–56 的 `recent` 数组仅存于 `this.data`，小程序被杀/切后台较长时间后即丢失。现场人员在巡检时经常需要重复扫同一批器具，每次都要重新扫码。 |
| 方案 | `recent` 改为 `wx.setStorageSync('scanHistory', recent)` 持久化（上限 20 条），`onLoad` 时从存储恢复。增加"清空历史"按钮。 |
| 效果 | 扫码历史跨会话保留，连续巡检场景效率提升明显。 |
| 难度 | **低** — 单文件 5 行改动 |
| 优先级 | **P1** |

### FEAT-05 | 领用/归还无批量操作
| 状态 | **✅ 已完成（batchBorrow/batchReturn 批量端点）** |

| 项 | 内容 |
| --- | --- |
| 问题 | `borrow/index.js` borrow 和 returnTool 均只支持单件操作。现场场景：工人从库房一次性领用 5 件器具（如安全带+头盔+手套+扳手+电钻），需要扫码 5 次、确认 5 次。 |
| 方案 | ① 新增 `batchBorrow(ids[])` 和 `batchReturn(ids[])` 端点：循环校验每件的资格（合格+有效期内+持证），失败项单独标记不阻塞成功项，返回 `{success: [], failed: [{id, reason}]}`；② 扫码页增加「批量模式」开关——开启后连续扫码自动加入待操作清单，一键提交。 |
| 效果 | 多件器具操作从 O(n) 次交互降为 1 次确认，显著提升库房/现场流转效率。 |
| 难度 | **中** — 需新增 2 个端点 + 前端批量 UI |
| 优先级 | **P1** |

### FEAT-06 | 消息预警跳转精度不足
| 状态 | **✅ 已完成（预警跳转携带 refId）** |

| 项 | 内容 |
| --- | --- |
| 问题 | `pages/message/message.js` L96–107 的 `ROUTES` 映射仅按 `refType` 跳转到列表页（如 scrap→报废审批列表），无法精确定位到具体的那条待审批报废记录。用户点击预警后还需在列表页再次搜索/翻页，预警到处置的路径过长。 |
| 方案 | 预警消息 `warnings` 集合增加 `refId` 字段（关联业务记录的 _id），前端跳转时带 `id` 参数直达详情/操作页。同时 warning 生成逻辑（`warning/index.js`）在创建预警时写入对应的业务 ID。 |
| 效果 | 点击预警 → 直接打开对应业务详情，处置路径从"预警列表→业务列表→搜索→详情"缩短为"预警→详情"。 |
| 难度 | **中** — 涉及 warning 生成逻辑 + 前端路由 |
| 优先级 | **P1** |

### FEAT-07 | 器具详情页缺少"快速操作"入口
| 状态 | **✅ 已完成（详情页上下文快捷操作栏，代码已具备）** |

| 项 | 内容 |
| --- | --- |
| 问题 | `pages/tool-detail/tool-detail` 展示器具完整档案（履历/检验记录/条码），但缺少上下文相关的快捷操作。如扫码后在详情页看到器具为"合格"状态，想直接领用需要退出→去领用页→再扫一次。 |
| 方案 | 在详情页底部增加浮动操作栏（按器具当前状态动态显示）：合格→"立即领用"按钮；领用中→"归还"+"外观检查"按钮；维修中→"查看维修进度"；待检→"提交试验"。点击直接调用已有 API，结果即时刷新页面状态。 |
| 效果 | 减少页面跳转，把高频操作路径从 3 步缩短到 1 步（AGENTS.md §1④"直取核心"）。 |
| 难度 | **低** — 单页面改动 |
| 优先级 | **P1** |

### FEAT-08 | 数据导出仅返回 JSON，无真实文件下载
| 状态 | **⏸ 待办（中/P2：云存储文件导出，建议下个迭代）** |

| 项 | 内容 |
| --- | --- |
| 问题 | `tool/index.js` `exportLedger` 返回 JSON rows，前端 `pages/ledger/ledger.js` 的导出逻辑在 JS 中构建 CSV 后通过 `wx.shareFileMessage` 或剪贴板分享。大数据量（>200 条）时 JS 字符串拼接耗内存且无格式支持（如 Excel .xlsx）。 |
| 方案 | ① 改用云函数生成 Excel/CSV 并上传到云存储，返回 `cloud://` fileID 供 `wx.cloud.downloadFile` + `wx.openDocument` 打开；② 支持筛选条件下导出（当前筛选条件透传到导出接口）。 |
| 效果 | 无数据量限制，原生文件打开体验，满足管理台账的月度归档需求。 |
| 难度 | **中** — 云存储 + 文件下载 |
| 优先级 | **P2** |

---

## 三、新功能拓展（5 项）

### NEW-01 | 智能维保预测
| 状态 | **⏸ 待办（中/P2：维保预测，建议下个迭代）** |

| 项 | 内容 |
| --- | --- |
| 背景 | 当前维保为被动响应模式：器具损坏才报修，或按固定周期（6 个月）安排检验。无法利用器具的历史使用数据（领用频次、损坏次数、外观检查记录）提前预警。 |
| 方案 | 新增云函数 `predictive`，基于器具的领用频次、历史损坏率、试验不通过率、类别（高空/电动工具更易损耗）四个因子计算「维保风险分」。在 dashboard 增加「需关注器具」卡片（高风险分 Top 10），预警自动纳入。纯规则引擎实现（因子加权 + 阈值触发），无需 ML。 |
| 收益 | 从"坏了才修"转向"提前保养"，降低器具使用中的安全事故风险。直接产出维保计划建议，减少人工排期遗漏。 |
| 难度 | **中** |
| 优先级 | **P2** |
| 实现基础 | 已有 borrow_records（领用频次）、repair 记录（损坏率）、testRecords（试验不通过率）、TOOL_CATEGORIES（类别） |

### NEW-02 | 离线扫码巡检工作流
| 状态 | **⏸ 待办（中/P1：离线巡检工作流，依赖 FEAT-02，建议下个迭代）** |

| 项 | 内容 |
| --- | --- |
| 背景 | 现场安全检查（日常检/专项检）依赖于网络：加载检查模板 → 拍照取证 → 提交。地下室/隧道场景网络中断时检查流程完全瘫痪。 |
| 方案 | ① `pages/site/daily-check` 改为离线优先：模板缓存在本地（含检查项 JSON），检查结果暂存 `wx.getStorageSync`；② 图片附件在本地压缩后暂存临时路径；③ 网络恢复时自动批量上传（`wx.onNetworkStatusChange` 触发）。借鉴 `utils/network.js` 的 `cacheThenNetwork` 模式。 |
| 收益 | 彻底解决施工现场无网无法检查的痛点。检查数据不丢失，恢复网络后自动同步。 |
| 难度 | **中** — 离线队列 + 自动重传 |
| 优先级 | **P1** |

### NEW-03 | 纸质标签打印集成
| 状态 | **⏸ 待办（高：标签打印，依赖蓝牙硬件适配）** |

| 项 | 内容 |
| --- | --- |
| 背景 | 当前条码生成仅返回二维码内容字符串和元数据（`genBarcode` / `barcodeFile`），前端自行渲染。现场实际需求是将器具信息 + 二维码直接打印到不干胶标签上（50×30mm / 70×50mm 等标准尺寸）。 |
| 方案 | `cloudfunctions/file` 增加 `printLabel` action：接收器具 ID 数组 + 标签尺寸参数，用 `canvas` 服务端渲染生成标签图片（含器具名称、编号、二维码、有效期、库房），上传云存储返回 `fileID`。前端 `wx.downloadFile` + `wx.openDocument` 或通过蓝牙打印机 SDK 直接输出。 |
| 收益 | 完成"器具建档→生成条码→打印标签→贴标上架"的完整闭环，取代目前的手写标签。 |
| 难度 | **高** — 服务端渲染 + 蓝牙打印适配 |
| 优先级 | **P2** |

### NEW-04 | 施工班组协作看板
| 状态 | **✅ 已完成（pkg-stats/pages/team 班组协作看板）** |

| 项 | 内容 |
| --- | --- |
| 背景 | 当前组织架构支持 单位→项目部→班组 三级，但信息按角色和 orgId 隔离——同一班组内看不到其他人的领用/归还动态。现场场景：班组长需要知道"哪些组员领了哪些器具、谁还没还"。 |
| 方案 | 新增 `pkg-stats/pages/team-dashboard`（班组看板）：① 按班组展示当前已领用器具清单（谁领的、领了多久、是否超期）；② 班组安全合规率（持证率、器具合规率）；③ 每日交接班器具核对清单。复用 `borrow_records` + `users.orgId` 已有的数据基础。 |
| 收益 | 班组长从"一个个问"变为"一眼看清"，减少器具丢失和超期未还。增强班组级管理能力。 |
| 难度 | **低** — 新页面 + 已有数据 |
| 优先级 | **P1** |

### NEW-05 | 消息推送：微信服务通知 + 订阅消息打通
| 状态 | **⏸ 待办（中/P1：微信订阅消息，依赖公众平台模板申请）** |

| 项 | 内容 |
| --- | --- |
| 背景 | 当前预警仅在站内消息页展示（`pages/message`），用户不打开小程序就不知道有新预警。`SUBSCRIBE_TMPL_ID` 为空表示微信订阅消息模板未配置。 |
| 方案 | ① 在微信公众平台申请「预警通知」订阅消息模板，填入 `utils/constants.js` 的 `SUBSCRIBE_TMPL_ID`；② `cloudfunctions/warning` 的 `generate` 在生成预警时，通过云开发「云调用」API（`cloud.openapi.subscribeMessage.send`）向已订阅用户推送微信服务通知；③ 消息页增加「推送设置」卡片（允许用户选择接收哪些类型的预警推送）。 |
| 收益 | 关键预警（试验超期、证书到期、报废审批）实时触达用户微信，不依赖小程序打开率。 |
| 难度 | **中** — 订阅消息模板申请 + 云调用 |
| 优先级 | **P1** |
| 前置依赖 | 需管理员在微信公众平台完成模板申请 |

---

## 四、总体优先级矩阵

| 优先级 | 类别 | 项目 | 工作量 | 依赖 |
| --- | --- | --- | --- | --- |
| **P0** | 安全 | OPT-02 限流全量接入 | 1h | 无 |
| **P0** | 安全 | FEAT-01 密码复杂度 | 30m | 无 |
| **P0** | 体验 | FEAT-03 多字段搜索 | 10m | 无 |
| **P1** | 体验 | FEAT-04 扫码历史持久化 | 15m | 无 |
| **P1** | 体验 | FEAT-07 详情页快捷操作 | 1h | 无 |
| **P1** | 功能 | FEAT-02 离线缓存 | 2h | 无 |
| **P1** | 功能 | FEAT-05 批量领用归还 | 2h | 无 |
| **P1** | 功能 | FEAT-06 预警跳转精度 | 1h | 无 |
| **P1** | 功能 | NEW-02 离线巡检工作流 | 3h | FEAT-02 |
| **P1** | 功能 | NEW-04 班组协作看板 | 1.5h | 无 |
| **P1** | 功能 | NEW-05 微信推送 | 2h | 模板申请 |
| **P2** | 功能 | FEAT-08 文件导出 | 1.5h | 无 |
| **P2** | 功能 | NEW-01 维保预测 | 3h | 无 |
| **P2** | 功能 | NEW-03 标签打印 | 4h | 蓝牙硬件适配 |
| — | 代码 | OPT-01 去重 | 1h | 无 |
| — | 代码 | OPT-03 缓存轮换 | 10m | 无 |
| — | 代码 | OPT-04 模板对齐 | 15m | 无 |
| — | 代码 | OPT-05 子树去重 | 15m | 无 |
| — | 代码 | OPT-06 常量收敛 | 15m | 无 |
| — | 代码 | OPT-07 真分页改造 | 2h | 云数据库确认 |

---

## 五、AGENTS.md 准则回检

| 准则 | 本次建议覆盖情况 |
| --- | --- |
| ① 治本不治标 | OPT-01/02/07 解决副本漂移、限流缺失、硬编码上限——均为系统性根因修复 |
| ② 通用可演进 | NEW-01/02/04 基于已有数据基础扩展，不破坏现有隔离层；OPT-04/06 让模板和常量本身可演进 |
| ③ 仓库即记忆 | 每项建议均标注文件行号 + 具体代码片段，下一个 Agent 可无缝接续 |
| ④ 直取核心 | P0/P1 排序依据实际业务影响（安全>体验>功能），不堆积低价值建议 |

---

*报告完成时间：2025-07-27 | 基于 AGENTS.md v1 + 全面代码审查 | 共 20 项建议*
